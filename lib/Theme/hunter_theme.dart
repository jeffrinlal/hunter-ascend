import 'package:flutter/material.dart';

class HunterTheme {
  static const background = Color(0xFFFAFAFA);
  static const backgroundTop = Color(0xFFFAFAFA);
  static const backgroundMid = Color(0xFFFFF0E8);
  static const primary = Color(0xFFFF6B2B);
  static const cardColor = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF666666);
  static const neonBlue = Color(0xFFFF6B2B); // kept same name so other screens don't break
}